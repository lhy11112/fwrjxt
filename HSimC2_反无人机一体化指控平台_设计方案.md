# HSimC2 反无人机一体化指挥控制平台 — 设计方案

## 文档概要

| 项目 | 内容 |
|------|------|
| 项目名称 | HSimC2 反无人机一体化指挥控制平台 |
| 文档版本 | V1.0 |
| 编制日期 | 2026-06-26 |
| 参考需求 | `HSimC2一体化指控平台.txt` |

---

## 第1章 参考系统分析与设计指导思想

### 1.1 国际主流 C-UAS C2 系统参考

| 系统 | 开发商 | 核心架构特征 | 可借鉴设计点 |
|------|--------|-------------|------------|
| **Anduril Lattice** | Anduril Industries（美） | 边缘 mesh 网络 + 开放 SDK + AI 驱动决策闭环 | 开放 API 设计（REST/gRPC）、多语言 SDK、传感器-效应器闭环 < 1s |
| **FreeTAKServer / ATAK** | 开源社区（美空军开源） | CoT 协议 + 插件架构 + 多平台客户端 | 轻量化态势感知协议设计、支持降级运行（off-grid） |
| **ParallaxOS** | Surtr Defense（YC孵化） | 开源反无人机操作系统，打破厂商锁定 | 传感器抽象层设计、AI 威胁分类 |
| **NATO IAMD C2** | 北约标准化体系 | SAPIENT 传感器接口标准 + 分层防御 + Link-16 集成 | 标准化接口协议、分层 C2 模式（5 种 C2 方法） |
| **Fortem DroneHunter** | Fortem Technologies | 雷达 + AI + 拦截器闭环 | 单目标-多目标拦截路径规划 |

### 1.2 开源项目参考

| 项目 | 技术栈 | 可复用能力 |
|------|--------|-----------|
| `cweekiat/CUAS` | Python, YOLOv8, Kalman | 视觉检测 + 跟踪 |
| `alireza787b/mavsdk_drone_show` (MDS) | Python, PX4, MAVSDK, React | 多无人机编队任务规划、SITL 仿真 |
| `mUDAI` | Python | 多无人机散点区域检查路径优化 |
| `FreeTAKServer` | Python, Flask, SQLAlchemy | 态势感知服务器、CoT 消息协议 |
| `PyTAK` | Python | 传感器数据→CoT 协议转换网关 |
| JRC C-UAS 平台 | Python, Redis, MQ | 多传感器轨迹融合（DTW） |

### 1.3 设计指导思想

```
借鉴 Lattice 的「开放生态 + 边缘计算」理念
融合 NATO 的「分层防御 + 标准化接口」体系
采纳 TAK 的「轻量化态势 + 降级运行」设计
复用开源项目的「传感器融合 + 路径规划」算法
```

---

## 第2章 总体架构设计

### 2.1 架构总览

本平台采用 **"四横两纵"** 分层架构：

```
┌─────────────────────────────────────────────────────────────────────┐
│                    HSimC2 一体化指挥控制平台                           │
│                     (Counter-UAV Integrated C2 Platform)              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  E. 可视化指挥与交互层 (Visual C2 & HMI)                      │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐│   │
│  │  │ GIS态势   │ │ 目标跟踪  │ │ 任务规划  │ │ 人机协同交互      ││   │
│  │  │ 可视化    │ │ 可视化    │ │ 可视化    │ │ (对话/推荐/修正)  ││   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘│   │
│  └───────────────────────────────────────────────────────────────┘   │
│                        ↕ REST/WebSocket/gRPC                          │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  D. 决策规划层 (Decision & Planning)                           │   │
│  │  ┌─────────────────┐ ┌──────────────────────────────────────┐ │   │
│  │  │ 威胁态势估计模块   │ │ 反无任务规划模块                       │ │   │
│  │  │ • 意图识别模型    │ │ • 单/多目标轨迹规划                   │ │   │
│  │  │ • 威胁等级评估    │ │ • 资源调度与冲突消解                   │ │   │
│  │  │ • 动态威胁排序    │ │ • 策略动态调整                        │ │   │
│  │  └─────────────────┘ └──────────────────────────────────────┘ │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                        ↕ 内部事件总线 / 消息队列                       │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  C. 融合感知层 (Fusion & Situational Awareness)               │   │
│  │  ┌──────────────────┐ ┌────────────────┐ ┌──────────────────┐ │   │
│  │  │ 多源数据融合       │ │ 多目标跟踪       │ │ 协同态势生成       ││   │
│  │  │ (雷达/RF/EO/声学) │ │ (多目标/多场景)  │ │ (识别/分类/标注)  ││   │
│  │  └──────────────────┘ └────────────────┘ └──────────────────┘ │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                        ↕ 标准化传感器抽象层                            │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  B. 数据接入与预处理层 (Data Ingestion & Preprocessing)        │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │   │
│  │  │ 雷达数据  │ │ RF射频   │ │ EO/IR    │ │ 声学/其他传感器    ││   │
│  │  │ 解析器    │ │ 解析器    │ │ 图像解析器│ │ 解析器            ││   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘ │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐                     │   │
│  │  │ 数据清洗  │ │ 去噪补全  │ │ 时间同步  │                     │   │
│  │  │          │ │ 动态校验  │ │ 空间对齐  │                     │   │
│  │  └──────────┘ └──────────┘ └──────────┘                     │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  A. 综合管理层 (Cross-cutting Management)                      │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │   │
│  │  │ 数据管理  │ │ 算法     │ │ 任务调度  │ │ 系统监控与        ││   │
│  │  │ (元数据/  │ │ 测试与   │ │ 与工作流  │ │ 运维管理          ││   │
│  │  │ 血缘/权限)│ │ 部署管理  │ │ 引擎      │ │ (健康/日志/告警)  ││   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘ │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │  安全与权限体系 (Security & RBAC)  │  标准化外部接口 (EIAPI)  │    │
│  └──────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 架构分层说明

| 层级 | 名称 | 职责 | 关键技术 |
|------|------|------|---------|
| A | 综合管理层 | 贯穿全栈：数据治理、算法生命周期、任务调度、系统监控 | PostgreSQL + Redis, K8s, DAG 工作流引擎 |
| B | 数据接入与预处理层 | 多传感器标准化接入、信号清洗、时空对齐 | gRPC/Protobuf 传感器抽象层、信号处理 Pipeline |
| C | 融合感知层 | 多源特征融合、多目标跟踪、协同态势生成 | EKF/UKF/JPDA, 深度学习融合模型 |
| D | 决策规划层 | 威胁态势估计、任务规划、策略优化 | POMDP, PSO 群体智能, 多约束路径规划 |
| E | 可视化指挥与交互层 | GIS 态势展示、任务可视化、人机协同交互 | CesiumJS/DeckGL, WebSocket 实时推图, LLM 对话 |

### 2.3 网络部署架构

```
                        ┌─────────────────────┐
                        │    指挥中心 C2 Server     │
                        │  (HSimC2 Core Engine)    │
                        └──────────┬──────────────┘
                                   │ MIDDLEWARE / FEDERATION
                                   │
          ┌────────────────────────┼────────────────────────┐
          │                        │                        │
   ┌──────▼──────┐          ┌──────▼──────┐         ┌──────▼──────┐
   │  边缘节点 1   │          │  边缘节点 2   │         │  边缘节点 N   │
   │ (前出部署)    │          │ (区域站点)    │         │ (移动指挥车)   │
   └──────┬──────┘          └──────┬──────┘         └──────┬──────┘
          │                        │                        │
   ┌──────▼──────┐          ┌──────▼──────┐         ┌──────▼──────┐
   │ 传感器集群   │          │ 效应器集群    │         │ 友邻系统接口  │
   │ (雷达/RF/   │          │ (干扰/拦截/   │         │ (NATO STANAG │
   │  EO/声学)   │          │  诱骗)       │         │  /Link-16)   │
   └─────────────┘          └─────────────┘         └─────────────┘
```

- **中心 C2 Server**：承担全局融合、决策规划、持久化存储、对外接口
- **边缘节点**：承载局部传感器融合、降级运行能力（断网自主决策）
- **传感器/效应器**：通过标准化 SDK 或协议适配器接入

---

## 第3章 模块级详细设计

### 3.1 数据接入与预处理层 (B层)

#### 3.1.1 标准化传感器抽象层 (SSAL)

借鉴 Anduril Lattice 的 Entities API 和 NATO SAPIENT 标准设计：

```
                    ┌────────────────────────────┐
                    │    HSimC2 Sensor SDK        │
                    │  (多语言: Rust/Python/C)     │
                    └────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         │                    │                    │
   ┌─────▼──────┐      ┌─────▼──────┐      ┌─────▼──────┐
   │ Radar       │      │   RF        │      │  EO/IR      │
   │ Adapter     │      │   Adapter   │      │  Adapter    │
   │             │      │             │      │             │
   │ • AESA      │      │ • SDR (USRP)│      │ • 可见光    │
   │ • FMCW      │      │ • 频谱分析   │      │ • 红外热像  │
   │ • 脉冲多普勒 │      │ • ADS-B     │      │ • 激光雷达  │
   └──────┬──────┘      └──────┬──────┘      └──────┬──────┘
          │                    │                    │
          └────────────────────┼────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   Sensor Bus        │
                    │  Protocol: gRPC/    │
                    │  Protobuf Streams   │
                    └─────────────────────┘
```

**传感器数据统一消息格式 (Protobuf Schema)：**

```protobuf
message SensorFrame {
  string sensor_id = 1;          // 传感器唯一ID
  string sensor_type = 2;        // RADAR | RF | EO | ACOUSTIC | LIDAR
  double timestamp = 3;          // Unix nano 时间戳
  double latitude = 4;           // WGS84
  double longitude = 5;
  double altitude = 6;           // 米
  // 载荷数据 (具体类型由 sensor_type + data_format 决定)
  oneof payload {
    RadarDetections radar = 10;
    RFSpectrum rf_spectrum = 11;
    ImageFrame image = 12;
    AcousticFeatures acoustic = 13;
    PointCloud lidar = 14;
  }
  Confidence confidence = 15;    // 置信度
}
```

#### 3.1.2 数据清洗与预处理管线 (Pipeline)

采用 **actor 模型** 构建流水线处理链（Rust 生态天然优势）：

```
原始数据 ──→ [去噪滤波器] ──→ [缺失补全] ──→ [动态校验] ──→ [时空对齐] ──→ [标准化输出]
                │                │              │               │
                ▼                ▼              ▼               ▼
          Kalman/LPF    插值/预测    范围/一致性    各传感器统一时间戳
          (传感器级)    (传感器级)   校验           + 空间坐标转换
```

| 处理阶段 | 算法选型 | 功能 |
|---------|---------|------|
| 去噪 | 卡尔曼滤波/小波变换/LPF | 消除设备系统误差与环境噪声 |
| 补全 | 线性插值/GP回归 | 填补短时信号中断造成的缺失数据 |
| 校验 | 范围检查 + 一致性校验 | 剔除超限/异常值（如：速度超过物理极限） |
| 时空对齐 | PTP/1588 + 坐标变换 | 统一到 GPS 时间基准 + WGS84 空间坐标系 |

#### 3.1.3 机型特征库 / 电磁频段数据库

- **存储**：PostgreSQL + TimescaleDB 时序扩展 + Redis Cache
- **数据结构**：
  - 机型库：`(机型ID, 型号, 类型, 尺寸, 最大速度, 典型RCS, 通信频段, IR特征, 声学特征, 威胁等级基线)`
  - 电磁频段库：`(频段ID, 起始频率, 终止频率, 典型用途, 协议类型[WiFi/OcuSync/Lightbridge/ADS-B], 关联机型列表)`
- **更新机制**：支持热更新、增量同步至边缘节点

---

### 3.2 融合感知层 (C层)

#### 3.2.1 多源数据融合引擎

采用 **JDL 融合模型** 的四级融合体系：

| 融合级别 | 名称 | 技术实现 | 输入 | 输出 |
|---------|------|---------|------|------|
| L0 | 信号级融合 | 直接信号关联（多雷达点迹融合） | 多雷达原始点迹 | 融合点迹 |
| L1 | 特征级融合 | 分布式 EKF/UKF + IMM 滤波器 | 点迹/检测框/RF特征 | 统一目标航迹 |
| L2 | 态势级融合 | 群体态势图生成（多目标关联） | 各目标航迹 | 空域整体态势图 |
| L3 | 威胁级融合 | 意图识别模型 + 威胁评估 | 态势图+情报数据 | 威胁等级+意图分类 |

**核心算法组件：**

```
┌─────────────────────────────────────────────────────────────────┐
│                    多源融合引擎 (Fusion Engine)                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────┐      ┌───────────────────────────────┐  │
│  │ 时空关联模块          │      │ 特征融合模块                      │  │
│  │ • GNN(全局最近邻)    │─────▶│ • 雷达特征 + RF 频谱特征融合       │  │
│  │ • JPDA(联合概率)     │      │ • EO/IR 图像特征融合              │  │
│  │ • MHT(多假设跟踪)    │      │ • 多模态深度学习融合模型            │  │
│  └─────────────────────┘      └───────────────┬───────────────┘  │
│                                                │                  │
│  ┌─────────────────────┐      ┌────────────────▼──────────────┐  │
│  │ 跟踪滤波模块          │      │ 目标分类模块                       │  │
│  │ • EKF/UKF           │      │ • YOLOv8/RT-DETR (EO/IR)      │  │
│  │ • IMM(交互多模型)    │      │ • RF指纹识别 (SDR频谱)           │  │
│  │ • 多目标航迹管理      │      │ • 声学特征CNN分类                 │  │
│  └─────────────────────┘      └───────────────────────────────┘  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.2.2 多目标监测与跟踪

参考 N 个目标的联合概率数据关联（JPDA）方法：

**关键技术指标：**
- 同时跟踪目标数：≥30 架
- 识别非大疆无人机：≥20 架
- 目标识别率：≥95%
- 探测距离：≥6km，探测概率 ≥90%

**算法实现路径：**
1. **检测层**：每类传感器独立检测（雷达点迹/YOLO检测框/RF信号指纹）
2. **关联层**：全局最近邻（GNN）作为初筛，JPDA/MHT 处理密集场景
3. **滤波层**：交互多模型（IMM）滤波器，在匀速/机动/悬停模型间切换
4. **管理层**：航迹起始/维持/消亡全周期管理（M/N逻辑）

**行为分类与威胁标注：**
```
行为分类模型：
  输入：航迹历史(位置/速度/加速度/机动频率) + 通信信号模式
  输出：[侦察 | 攻击 | 诱饵 | 监视 | 运输 | 其他]
        威胁等级：[RED | YELLOW | GREEN]
  
分类器选型：LightGBM + LSTM 时序分类 + 规则引擎（可解释性保障）
```

#### 3.2.3 协同态势生成

- **多节点协同**：基于 DDS（Data Distribution Service）在中心+边缘间分发态势
- **GIS 融合**：态势数据流式写入 PostgreSQL PostGIS，前端实时订阅
- **一致性保障**：基于 Raft/CRDT 的多节点航迹去重与状态收敛

---

### 3.3 决策规划层 (D层)

#### 3.3.1 威胁态势估计模块

采用 **认知式 C2** 的 OODA 环设计：

```
                  ┌─────────────┐
    传感器 ──────▶│   Observe    │
                  └──────┬──────┘
                         ▼
                  ┌─────────────┐
                  │   Orient    │
                  │ (意图识别 +  │
                  │  威胁评估)   │
                  └──────┬──────┘
                         ▼
                  ┌─────────────┐
                  │   Decide    │
                  │ (威胁排序 +  │
                  │  目标分配)   │
                  └──────┬──────┘
                         ▼
                  ┌─────────────┐
                  │    Act      │
                  │ (任务规划 +  │
                  │  指令下达)   │
                  └─────────────┘
```

**意图识别模型方案：**

| 方法 | 优势 | 适用场景 |
|------|------|---------|
| **LSTM/GRU + Attention** | 时序建模 + 关键行为捕捉 | 基础意图识别（优先级最高） |
| **Transformer Encoder** | 长程依赖建模 | 复杂战术意图（长航时行为序列） |
| **POMDP** | 部分可观马尔可夫决策过程 | 不确定性环境下的推断 |
| **规则引擎 (Drools/SRE)** | 可解释性 + 确定性 | 紧急场景快速判断（冷启动） |

**推荐方案：** 混合模型 — 规则引擎兜底 + LSTM-Attention 模型主推 + POMDP 处理高不确定性场景。

**动态威胁评估公式：**

```
ThreatScore = w₁ * IntentCoeff + w₂ * DistanceCoeff + w₃ * SpeedCoeff 
            + w₄ * AltitudeCoeff + w₅ * ModelThreatCoeff + w₆ * ManeuverCoeff
  
威胁等级：
  RED:    ThreatScore ≥ 0.75  (立即拦截)
  Yellow: 0.4 ≤ ThreatScore < 0.75  (密切监视)
  GREEN:  ThreatScore < 0.4   (常规监视)
```

- **指标要求**：评估准确率 ≥ 90%，响应时间 ≤ 2s（从侦测到威胁评估）
- **人机协同修正**：支持指挥员手动调整权重，AI 自动重新排序

#### 3.3.2 反无任务规划模块

**规划框架：**

```
┌──────────────────────────────────────────────────────────────┐
│                    反无任务规划引擎                              │
│                    C-UAS Mission Planner                       │
├──────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌─────────────────┐   ┌─────────────────┐                    │
│  │ 单目标拦截规划     │   │ 多目标拦截规划     │                    │
│  │ (轨迹实时重规划)   │   │ (任务分配+协同轨迹) │                    │
│  └────────┬────────┘   └────────┬────────┘                    │
│           │                     │                             │
│           └─────────┬───────────┘                             │
│                     ▼                                         │
│  ┌──────────────────────────────────────────┐                │
│  │ 路径优化与冲突消解模块                        │                │
│  │ • RRT* / Hybrid A* / Dubins曲线           │                │
│  │ • 多约束：NO-FLY / 地形/设备性能/时间最优    │                │
│  │ • 冲突消解：优先级+速度调节+轨迹调整          │                │
│  └──────────────────┬───────────────────────┘                │
│                     ▼                                         │
│  ┌──────────────────────────────────────────┐                │
│  │ 资源调度与协同分配模块                        │                │
│  │ • 群体智能算法 (PSO/ACO) 任务分配           │                │
│  │ • 多类型效应器协同 (干扰/诱骗/动能拦截)       │                │
│  │ • 资源状态感知 + 动态重新分配                │                │
│  └──────────────────────────────────────────┘                │
│                                                                │
└──────────────────────────────────────────────────────────────┘
```

**算法选型：**

| 规划类型 | 算法 | 性能要求 |
|---------|------|---------|
| 单目标轨迹规划 | RRT*-Connect / Hybrid A* | ≤ 0.01s（突发目标） |
| 多目标轨迹规划 | 改进 ACO + 分布式约束优化 | ≤ 3s（饱和攻击） |
| 路径可行性保障 | 地形碰撞检测 + 动力学可行性校验 | ≥ 98% |
| 冲突消解 | 优先级协议 + 速度障碍法(VO) | ≥ 98% 成功率 |
| 任务分配 | 群体智能（改进 PSO） + 匈牙利算法 | 支持 ≥ 10 架拦截器 |
| 策略更新 | 热更新机制（不停机加载） | 周期 ≤ 1h |

**多目标任务分配与协同规划策略（三类策略）：**

| 策略 | 适用场景 | 核心方法 |
|------|---------|---------|
| **集中式分配** | 开阔场景、全局最优 | 混合整数线性规划 (MILP) |
| **分布式协商** | 复杂山地场景、通信受限 | 合同网协议 (CNP) + 分布式约束优化 |
| **分层混合** | 大规模蜂群对抗 | 上层集中分配 + 下层分布式执行 |

**场景适应性：**
- **开阔场景** → 聚焦路径时间最优，RRT* 全局搜索
- **复杂山地场景** → 地形高程 DEM 建模 + 多层 A* 避障
- **电磁对抗环境** → 考虑干扰区域作为禁飞区 + 通信中继规划

#### 3.3.3 策略动态调整机制

```
触发条件：环境突变（新增干扰源/天气恶化/目标逃逸/资源变化）
         ↓
    重新评估当前任务状态
         ↓
    方案一：局部修复 ←──────── 当前策略仍可用？
         ↓                          ↓ 否
    方案二：全局重新规划              └─→ 完全重规划（切换策略）
         ↓
    验证新策略可行性（仿真推演）
         ↓
    确认 → 下达新指令 → 可视化通知指挥员 → 人工确认/修正
```

---

### 3.4 可视化指挥与交互层 (E层)

#### 3.4.1 GIS 引擎选型

| GIS 引擎 | 优势 | 劣势 | 推荐场景 |
|---------|------|------|---------|
| **CesiumJS** | 3D Tiles 支持、全球地形、开源 | 重型加载 | **主力推荐**（三维空域态势） |
| **Mapbox GL JS** | 高性能 2.5D、矢量切片 | 商用许可证限制 | 辅助二维态势图 |
| **Leaflet** | 轻量级、插件丰富 | 无原生 3D | 移动端/轻量化部署 |
| **DeckGL + Mapbox** | WebGL 大规模点云渲染 | 学习曲线陡 | 多目标轨迹高性能渲染 |

**推荐方案：CesiumJS（主） + DeckGL/WebGL 图层叠加（高性能轨迹）**

#### 3.4.2 实时数据流架构

```
传感器数据 → 后端 Fusion Engine
                    ↓ (Protobuf 编码)
                WebSocket Cluster (NATS/Redis PubSub)
                    ↓
            ┌───────┴───────┐
            │               │
        ┌───▼───┐      ┌───▼───┐
        │ Client 1 │      │ Client N │
        │ (C2中心)  │      │ (移动端)  │
        └─────────┘      └─────────┘
```

- **消息协议**：Protobuf over WebSocket（低延迟 < 0.5s）
- **差分更新**：只推送变化量（位置/状态/威胁），而非全量态势
- **加载策略**：LOD（Level of Detail）— 远距离只显示概要，近距显示细节
- **图层管理**：10+ 类图层（雷达覆盖/目标轨迹/禁飞区/效应器状态/天气等）

#### 3.4.3 人机协同交互

借鉴 ATAK 的对话式协作理念：

| 交互模式 | 实现方式 | 使用场景 |
|---------|---------|---------|
| 图形化交互 | 点击/拖拽/右键菜单 | 常规操作（查看目标/创建任务/调整参数） |
| 对话式交互 | LLM + 指令解析 | "显示5km内所有红色威胁目标" |
| 协同决策 | AI 建议 + 人工确认 | 威胁等级修正、拦截方案选择 |
| 模板化指令 | 预设任务模板 + 快速下达 | 紧急场景一键响应 |

**对话式交互技术路线：**
```
用户自然语言输入
    ↓
LLM 解析 → 意图识别 + 参数提取
    ↓
映射到平台内部指令 (Intent → Action)
    ↓
执行并返回结果 → 预览确认 → 正式执行
```

#### 3.4.4 界面布局设计（参考布局）

```
┌─────────────────────────────────────────────────────────────┐
│  顶部栏：系统状态 / 告警 / 用户 / 模式切换 (日/夜/隐蔽)       │
├──────────┬────────────────────────────────┬──────────────────┤
│ 左侧面板  │        中央 GIS 态势区域         │ 右侧面板          │
│          │                                │                  │
│ 图层控制  │    CesiumJS 3D 控件             │ 目标列表          │
│ 传感器    │   • 地形可视化(高程DEM)          │ (威胁排序)        │
│ 状态面板  │   • 目标轨迹渲染                 │                  │
│          │   • 拦截路径渲染                  │ 选定目标详情      │
│ 任务列表  │   • 传感器覆盖显示                │ (航迹/速度/意图)   │
│          │   • 禁飞区/干扰区标注              │                  │
│ 快捷工具  │   • 天气图层 overlay              │ 操作面板          │
│          │                                │ (拦截/干扰/标注)   │
│          │                                │                  │
├──────────┴────────────────────────────────┴──────────────────┤
│  底部栏：时间轴 / 事件日志 / 通信状态 / 处理能力统计           │
└─────────────────────────────────────────────────────────────┘
```

---

### 3.5 综合管理层 (A层)

#### 3.5.1 数据管理子系统

| 模块 | 技术实现 | 功能 |
|------|---------|------|
| 元数据管理 | Apache Atlas / OpenMetadata | 数据分类、血缘追踪 |
| 权限控制 | RBAC + ABAC 混合模型 | 基于角色+属性的细粒度权限 |
| 动态更新 | Postgres LISTEN/NOTIFY + CDC | 数据变更实时推送 |
| 归档策略 | 冷热分层（SSD/HDD/对象存储） | 热数据 7 天 / 温数据 30 天 / 冷数据按需恢复 |

#### 3.5.2 算法测试与管理

```
                      ┌──────────────────────┐
                      │   算法仓库 Registry     │
                      │   (Git + Container)    │
                      └──────────┬───────────┘
                                 │
   ┌─────────┐    ┌─────────┐   │    ┌─────────┐   ┌─────────┐
   │版本管理   │    │性能评估   │◀──┴──▶│参数调优   │   │部署管理   │
   │(Git DVC) │    │(基准测试) │        │(Optuna) │   │(K8s)    │
   └──────────┘    └──────────┘        └──────────┘   └──────────┘
```

- **版本控制**：Git + DVC（数据版本控制）用于算法和数据版本管理
- **性能评估**：自动回归测试，对比基线指标（准确率/召回率/延迟/吞吐）
- **参数调优**：Optuna/Ray Tune 超参优化框架
- **部署管理**：Docker 容器化 + K8s 编排（支持蓝绿部署、A/B 测试）

#### 3.5.3 任务调度与监控

- **调度引擎**：基于 DAG 的任务编排（Airflow / Temporal）
- **资源监控**：Prometheus + Grafana（GPU/CPU/内存/网络）
- **异常处理**：自动重试 + 降级策略 + 告警通知

---

## 第4章 关键技术栈建议

### 4.1 整体技术栈

| 层次 | 技术选型 | 选型理由 |
|------|---------|---------|
| **核心语言** | Rust | 性能、内存安全、并发（项目已有 Rust 基础） |
| **传感器 SDK** | Rust (core) + Python (binding) | Rust 核心性能 + Python 数据科学生态 |
| **前端** | React + TypeScript + CesiumJS | 成熟生态、3D GIS 支持 |
| **后端框架** | Actix-web (Rust) / Axum | 高并发、WebSocket 原生支持 |
| **消息队列** | NATS / Redis Streams | 低延迟、PubSub 模式、去中心化 |
| **数据库** | PostgreSQL + TimescaleDB + PostGIS | 时序数据 + 空间查询 |
| **缓存** | Redis | 实时态势缓存、会话管理 |
| **ML 框架** | PyTorch (ONNX Runtime serving) | 深度学习模型训练 + 部署 |
| **GIS 引擎** | CesiumJS + GDAL (数据处理) | 3D Tiles、空间分析 |
| **计算图/调度** | Temporal / Airflow | 可靠的任务编排与调度 |
| **容器 & 编排** | Docker + K8s (K3s 边缘版) | 边缘部署 轻量化 |
| **通信协议** | gRPC/Protobuf + WebSocket | 高性能序列化、双向流 |

### 4.2 Rust 生态优势在 HSimC2 中的应用

```
┌──────────────────────────────────────────────────────────┐
│                 Rust 在 HSimC2 中的角色                    │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  HSimC2 Core Engine (Rust)                               │
│  ┌────────────────────────────────────────────────────┐  │
│  │ • 传感器数据采集与预处理管线 (性能关键路径)           │  │
│  │ • 多目标跟踪滤波引擎 (EKF/UKF/JPDA 实时计算)        │  │
│  │ • 轨迹规划核心算法 (RRT*/Hybrid A*)                 │  │
│  │ • 消息总线与实时数据分发                             │  │
│  │ • 嵌入式/边缘部署 (no-std 友好)                     │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  选型理由：                                               │
│  1. 零成本抽象 — 算法密集型任务的极致性能                 │
│  2. 内存安全 — 消除数据竞争和内存泄漏 (可靠性关键系统)    │
│  3. 异步生态 — Tokio 提供高性能异步 I/O                 │
│  4. 嵌入式支持 — 可直接部署在传感器边缘端                 │
│  5. WASM 编译 — 算法可编译为 WASM 在浏览器端运行        │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## 第5章 外部接口与互操作性设计

### 5.1 标准化外部接口 (EIAPI)

借鉴 Anduril Lattice 的三层 API 模型：

| API | 协议 | 用途 | 参考来源 |
|-----|------|------|---------|
| **Entities API** | REST/gRPC | 实体（目标/航迹/传感器/效应器）CRUD 操作 | Lattice SDK |
| **Tasks API** | gRPC Stream | 任务下发、状态追踪、指令生命周期管理 | Lattice SDK |
| **Alerts API** | WebSocket | 实时告警推送 | — |
| **Export API** | REST + File | 数据导出、报告生成、日志归档 | — |
| **Federation API** | gRPC | 多平台互联（HSimC2 ↔ 友邻 C2 系统） | NATO STANAG |

### 5.2 协议适配器

```
外部系统 ──→ [协议适配器网关] ──→ 内部统一消息格式
                 │
    ┌────────────┼────────────┐
    │            │            │
  Link-16    STANAG  UDP/TCP
  (战术数据链)   (NATO)    (私有协议)
```

---

## 第6章 实施路线图

### 阶段一：基础架构与核心模块 (第1-6个月)

| 里程碑 | 交付物 | 关键指标 |
|-------|--------|---------|
| M1 | Rust 核心引擎框架搭建 | 传感器数据接入 Pipeline 原型 |
| M2 | 标准化传感器 SDK v1 | 支持 3 类传感器（雷达/RF/EO）接入 |
| M3 | 多目标跟踪基础版 | 单目标 + 10 目标跟踪演示 |
| M4 | 基础 GIS 可视化原型 | CesiumJS 地图 + 目标轨迹展示 |
| M5 | 综合管理模块 | 数据管理 + 算法管理基础功能 |

### 阶段二：融合与决策能力 (第7-12个月)

| 里程碑 | 交付物 | 关键指标 |
|-------|--------|---------|
| M6 | 多源融合引擎 | 特征级融合 + 目标识别率 ≥ 90% |
| M7 | 威胁态势估计 | 意图识别 + 威胁分级，准确率 ≥ 85% |
| M8 | 单目标轨迹规划 | 响应 ≤ 0.01s |
| M9 | 集成测试 v1 | 探测距离 ≥ 6km，探测概率 ≥ 90% |

### 阶段三：高级协同与系统完善 (第13-18个月)

| 里程碑 | 交付物 | 关键指标 |
|-------|--------|---------|
| M10 | 多目标协同规划 | ≥ 10 架拦截器轨迹规划，冲突消解 ≥ 98% |
| M11 | 三类协同策略完整实现 | 开阔/复杂山地场景，热更新 ≤ 1h |
| M12 | 人机协同交互 | 对话式交互 + 协同决策 |
| M13 | 集成测试 v2 | 全部指标达标（识别率 ≥ 95%，拦截成功率 ≥ 90%） |
| M14 | 系统联调与验收 | 全链路贯通，性能达标 |

### 阶段四：智能化演进与生态构建 (第19-24个月)

- 群体智能深度优化（多蜂群对抗场景）
- 数字化孪生训练环境（数据驱动策略优化）
- 与友邻系统联邦互操作验证
- 开放 SDK 发布，第三方集成支持

---

## 第7章 关键技术风险与对策

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 多传感器时间同步精度不足 | 融合航迹偏差 | PTP 1588 硬件时间同步 + 软件后补偿 |
| 复杂电磁环境下探测概率下降 | 目标丢失 | 多传感器冗余 + 自适应切换阈值 |
| ML 模型可解释性不足 | 指挥员不信任 | 规则引擎兜底 + SHAP 可解释性输出 |
| 实时性不达标（轨迹规划 > 3s） | 无法应对蜂群攻击 | 优化前：Rust 实现 + GPU 加速；优化后：降级到启发式方案 |
| 边缘节点断网自主决策不足 | 降级能力缺失 | 边缘缓存模型 + 本地规则引擎 |

---

## 第8章 参考系统对照表

| 本模块 | 对标系统 | 借鉴设计 |
|--------|---------|---------|
| 数据接入层 | Lattice Entities API | Protobuf 统一传感器数据模型 |
| 融合感知层 | JRC C-UAS 平台 | DTW 航迹融合 + Redis 实时数据 |
| 威胁态势估计 | Lattice AI + NATO SAPIENT | 意图识别模型 + 标准化威胁评估 |
| 任务规划 | Fortem DroneHunter + mUDAI | 单目标/多目标路径规划算法 |
| 态势可视化 | ATAK CoT + CesiumJS | 实时态势推送 + 3D GIS 展示 |
| 人机交互 | ATAK + Lattice SDK | 对话式交互 + 建议-确认机制 |
| 管理后台 | FreeTAKServer | DAG 任务调度 + 算法版本管理 |
| 外部接口 | Lattice SDK + NATO STANAG | 三层 API 模型 + 协议适配器 |

---

## 附录：文件结构建议

```
HSimC2/
├── docs/                   # 设计文档
│   └── HSimC2_反无人机一体化指控平台_设计方案.md
├── src/                    # Rust 核心引擎
│   ├── core/               # 核心框架
│   │   ├── pipeline/       # 数据预处理管线
│   │   ├── fusion/         # 多源融合引擎
│   │   ├── tracker/        # 多目标跟踪
│   │   └── planner/        # 任务规划
│   ├── sensors/            # 传感器抽象层
│   │   ├── radar/          # 雷达适配器
│   │   ├── rf/             # 射频适配器
│   │   ├── eoir/           # 光电/红外适配器
│   │   └── acoustic/       # 声学适配器
│   ├── c2/                 # 指挥控制逻辑
│   │   ├── threat/         # 威胁评估
│   │   ├── tasking/        # 任务分配
│   │   └── dispatch/       # 指令分发
│   ├── bus/                # 消息总线
│   └── api/                # 外部接口 (gRPC/REST)
├── frontend/               # React + CesiumJS 前端
│   ├── src/
│   │   ├── components/
│   │   ├── gis/            # GIS 可视化
│   │   ├── chat/           # 对话式交互
│   │   └── store/          # 状态管理
│   └── public/
├── sdk/                    # 传感器 SDK
│   ├── rust/               # Rust SDK
│   ├── python/             # Python binding
│   └── proto/              # Protobuf 定义
├── services/               # 边缘/后端服务
│   ├── management/         # 综合管理服务
│   ├── scheduler/          # 任务调度服务
│   └── gateway/            # 协议适配网关
├── ml/                     # 机器学习模块
│   ├── models/             # 模型定义
│   ├── training/           # 训练脚本
│   └── inference/          # 推理服务
├── deploy/                 # 部署配置
│   ├── k8s/                # K8s 编排
│   ├── edge/               # 边缘节点配置
│   └── docker/             # Docker 镜像
└── tests/                  # 集成测试
    ├── simulation/         # 仿真测试
    └── integration/        # 硬件集成测试
```

---

> **文档版本**: V1.0 | **编制日期**: 2026-06-26  
> **参考需求**: `HSimC2一体化指控平台.txt`  
> **参考系统**: Anduril Lattice, NATO C2/IAMD, ATAK/FreeTAKServer, Surtr ParallaxOS, JRC C-UAS
