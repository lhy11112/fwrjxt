<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.system.mapper.SysUserMapper">

    <!-- 单机版本用户名查询 -->
    <select id="getUserByName" resultType="org.jeecg.modules.system.entity.SysUser">
        select a.id
             , a.YHM    as username
             , a.xm     as realname
             , a.BDNM   as departIds
             , a.YHJBNM as yhjbnm
             , b.bdxh
        from (select * from wjbd_mh_yh where yhm = #{username} and LJSC = 0) a
                 left join wjbd_mh_bd b
                           on a.bdnm = b.bdnm
                           </select>
    <!-- 对接单点登陆版本用户名查询 -->
    <select id="getUserByNames" resultType="org.jeecg.modules.system.entity.SysUser">
        select a.id
             , a.YHM    as username
             , a.xm     as realname
             , a.BDNM   as departIds
             , a.YHJBNM as yhjbnm
             , b.bdxh
        from (select * from wjbd_dxyy.wjbd_mh_yh where yhm = #{username} and LJSC = 0) a
                 left join wjbd_dxyy.wjbd_mh_bd b
                           on a.bdnm = b.bdnm
    </select>
</mapper>