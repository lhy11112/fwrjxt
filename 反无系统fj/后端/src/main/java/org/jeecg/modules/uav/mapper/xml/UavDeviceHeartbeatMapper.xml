<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.uav.mapper.UavDeviceHeartbeatMapper">
    <!-- 查询设备最新心跳 -->
    <select id="selectLatestHeartbeat" resultType="org.jeecg.modules.uav.entity.UavDeviceHeartbeat">
        SELECT * FROM uav_device_heartbeat
        WHERE station_id = #{stationId}
        ORDER BY data_time DESC
        LIMIT 1
    </select>

    <!-- 查询设备状态异常记录 -->
    <select id="selectAbnormalStatus" resultType="org.jeecg.modules.uav.entity.UavDeviceHeartbeat">
        SELECT * FROM uav_device_heartbeat
        WHERE station_id = #{stationId}
          AND (main_card = 1 
               OR trap_card = 1 
               OR compass = 1 
               OR disturb_card = 1)
          AND data_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY data_time DESC
    </select>
</mapper>
    